name: ci

on:
  schedule:
    - cron: "0 * * * *"
  push:
    branches:
      - main
jobs:
  cypress-tests:
    runs-on: ubuntu-latest
    container:
      image: cypress/browsers:node16.16.0-chrome108-ff107
      options: --user 1001
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: npm install
      - name: Run Cypress tests
        run: npm run cy:run
